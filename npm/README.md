# OnionCry

OnionCry is a CLI for checking architectural boundaries in JavaScript and
TypeScript projects.

```bash
npx onioncry --help
```

This package is a small launcher. It installs the matching native binary through
an optional platform package such as `@onioncry/cli-linux-x64`.
